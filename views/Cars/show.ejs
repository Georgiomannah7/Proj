<%- include('../includes/head.ejs') %>
</head>
<body>
    <%- include('../includes/nav.ejs') %>

    <% if(cars.length > 0){ for (let car of cars) {%>
            <h2><%= car.title%></h2>
            <h2><%= car.description%></h2>
            <h2><%= car.NbrOfRent%></h2>
            <h2><%= car.Image%></h2>
            <h2><%= car.brandName%></h2>
            <h2><%= car.categoryName%></h2>
        <% } }else{%>
            <h1>Not found</h1>
            <% } %>
    
<%- include('../includes/end.ejs') %>    